---
version: "2"

sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "database"
        out: "../../database"
        emit_json_tags: true
        emit_exact_table_names: true